<form method="post" action="{{ path('user_favorite_delete', {'favorite': user.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_favorite' ~ user.id) }}">
    <button class="btn btn-link btn-sm"><i class="fas fa-star"></i> {% trans %}remove.from.favorites{% endtrans %}</button>
</form>
