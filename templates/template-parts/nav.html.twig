<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-4">
    <div class="container">
        <a id="logo" class="navbar-brand" href="{{ path('homepage', {'_locale' : app.request.locale}) }}"><i class="fas fa-users mr-2"></i> {{ app.request.server.get('SERVER_NAME')|upper }}</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler"
                aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarToggler">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('post_index', {'_locale' : app.request.locale}) }}">
                        <i class="fas fa-file-alt"></i> {% trans %}posts{% endtrans %}
                    </a>
                </li>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarUsersDropdown" role="button"
                           data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-user"></i> {% trans %}users{% endtrans %}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarUsersDropdown">
                            <a class="dropdown-item" href="{{ path('user_index', {'_locale' : app.request.locale}) }}">{% trans %}all{% endtrans %}</a>
                            <a class="dropdown-item" href="{{ path('user_index', {'_locale' : app.request.locale}) }}">{% trans %}online{% endtrans %}</a>
                            <a class="dropdown-item" href="{{ path('user_index', {'_locale' : app.request.locale}) }}">{% trans %}new{% endtrans %}</a>
                        </div>
                    </li>
                </ul>
            </ul>

            <form method="get" class="form-inline my-2 my-lg-0 position-relative">
                <input class="form-control" type="search" name="search" placeholder="{% trans %}search{% endtrans %}" aria-label="search">
                <button id="search-submit" class="btn text-dark position-absolute" type="submit">
                    <i class="fas fa-search"></i>
                </button>
            </form>

            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarLocaleDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-globe"></i>
                        {% trans %}language{% endtrans %}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarLocaleDropdown">
                        <a class="dropdown-item" href="{{ path('homepage', {'_locale' : 'de'}) }}"><i class="fas fa-language"></i> {% trans %}german{% endtrans %}</a>
                        <a class="dropdown-item" href="{{ path('homepage', {'_locale' : 'en'}) }}"><i class="fas fa-language"></i> {% trans %}english{% endtrans %}</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
