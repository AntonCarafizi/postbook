<div class="card-footer d-flex justify-content-start">
    {% if app.user.id == user.id %}
        <a class="btn btn-link btn-sm" role="button" href="{{ path('user_edit', {'id': user.id, 'name': user.name|lower ~ '.' ~ user.surname|lower }) }}">
            <i class="far fa-edit"></i> {% trans %}edit{% endtrans %}
        </a>
        {{ include('user/_delete_form.html.twig') }}
    {% else %}
        {{ include('user/favorites/_form.html.twig') }}
    {% endif %}
</div>